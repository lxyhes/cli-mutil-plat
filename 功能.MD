Agent Teams — 多 AI 去中心化协作

Telegram 远程控制 — 手机远程操控 AI 会话

飞书集成 — 企业 IM 通知与控制

自主规划引擎 — LLM 自动分解任务并执行

会话摘要 — LLM 驱动的对话总结

智能建议 — 上下文感知命令推荐

工作流编排 — DAG 可视化编辑与执行

在线聊天 — 团队实时沟通与协作

OpenAI 兼容 API — 接入自定义 API 端点

任务评估 — 团队任务质量评估与评审状态展示

Goal Anchor — 目标锚定、交付物与漂移护栏可视化

定时任务 — 周期执行任务与运行历史管理

Prompt Optimizer — 基础版提示词优化与回合控制

Prompt Optimizer Advanced — 高级版提示词优化与深度分析能力



⏺ 这是个很好的问题。程序员日常最头疼的几件事：

1. 上下文切换 — 同时做多件事，切换回来就忘了做到哪了
2. 踩过的坑忘得快 — 同一个错误下次又踩，方案没积累
3. 任务做到一半 — 被打断后很难快速回到状态
4. 决策没记录 — 当时为什么这么设计，过几天就忘了

结合你现有的系统（会话、任务、目标、技能、团队），我建议落地方向是 "会话级工作记忆"：

▎ 每个会话关联一个轻量的工作上下文（当前任务、遇到的问题、已做的决策、待办），切换回来时一眼就能接上。

这个你有感觉吗？还是你有其他更痛的点想解决？    




每次让 Claude/Cursor 做代码审查，它都要把整个仓库重新读一遍。

500 个文件吞一遍，5000 个文件再吞一遍。

改了一个函数，它要把所有文件过一遍才能告诉你影响范围。

这不叫智能，这叫暴力。

code-review-graph 干了一件特别漂亮的事： 用 Tree-sitter 把代码解析成结构图谱（函数、类、调用链、依赖关系），存本地 SQLite。

每次文件变更，只更新受影响的部分。

核心是"爆炸半径"分析： 改了一个函数，自动追踪所有调用方、依赖方、关联测试。 AI 只读它需要读的文件。